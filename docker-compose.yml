services:
  postgres:
    image: postgres:16-alpine
    container_name: wibe-crawler-postgres
    environment:
      POSTGRES_DB: wibe_crawler
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
